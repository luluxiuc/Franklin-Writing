# 路线图

**这个路线图有两个入口:补研究,和做工具。** 前者更重要——这个项目的每一个设计决定都来自实测数据;数据不够的地方,决定就只能靠猜。

标注说明:**难度** 是指对一个不熟悉代码库的贡献者而言。带 ⭐ 的是最需要帮助的。

---

## 一、研究:把没测的测完

这一节里的每一条都对应研究报告中明确记录的缺陷。**它们不是锦上添花,是结论的软肋。**

### 1. 真人标注(人类一致率)⭐ 最重要

**缺什么:** 实验一的裁判**全部是 LLM**,这构成循环——测出来的是模型的自一致性,不是与人类专家的一致性。预注册文件里写明了这项指标,但从未收集。

**为什么重要:** 这是唯一能回答"这个判断在原理上是否可自动化"的数据。如果**人类之间本身就不一致**,那么"对齐人类共识"这件事就没有对象,结论从"当前模型不行"升级为"这条路本身不通"。

**怎么做:**
- 工具已经备好:[`docs/研究/实验数据/human_kit.py`](docs/研究/实验数据/human_kit.py)
- 它能生成**不含答案**的标注材料,收集标注,算出人类两两一致率,并与 LLM 裁判对照
- **需要 3 位以上合格读者各标一次**(合格 = 有稳定阅读量、能说清自己为什么这样判断)
- 语料需自行从公开来源获取(仓库不分发受版权保护的原文)

**产出:** 一份标注结果 + 人类一致率与 LLM 一致率的对比。

### 2. 非名篇语料复测 ⭐ 高价值

**缺什么:** 5 位裁判中有 3 位**违规检索并核实了出处**(协议明确要求不得推测作者身份)。所以"真实文本 0% 误判"不能完全归功于风格辨识能力。

**为什么重要:** 用户的真实需求恰恰是**非名篇语料**——他自己喜欢的、还没出名的、当代的作者。名篇下都有捷径可走,非名篇下能力只会更弱。

**怎么做:** 换一位模型没有记忆的当代作者,重跑实验一。**预期纯净判断能力更低**——但那才是真实能力。

**产出:** 两组语料的结果对比。

### 3. 保留原始日志重跑实验一

**缺什么:** 5 位裁判中只有 3 位的逐块判定有留档,裁判 2、4 的裁决只存在于报告表格里;而且裁决值是**人工转录的常量**,不是从采集日志自动读出来的。

**为什么重要:** 现在的核对只能证明"转录与报告自洽",不能证明"转录与模型当时的原始输出一致"。

**怎么做:** 重跑一遍,**保留每一份原始返回**。把判定直接落盘,不经人手。

### 4. 执行实验二(单句判别)

**缺什么:** 实验一是整段归属判断,它**无法把"风格判断"与"篇幅/信息量判断"分离开**。

**怎么做:** 把作者原句与"**同内容、同字数、同名物、只换写法**"的改写配对做二选一,并置换专名以阻断检索通道。设计已经写好:[`docs/研究/experiment-2-design.md`](docs/研究/实验一结果与实验二设计.md)。

**注意设计里写明的一条限制:** 材料由谁构造很关键。**由同一个人既造材料又设计实验,就无法区分"认出了作者的笔法"与"认出了实验者的笔法"。** 理想情况是换一位写作者来造材料。

### 5. 换一位写作者重造材料

**缺什么:** 实验一的模仿稿全部由执行者撰写,必然带执行者自己的风格指纹。这是结构性限制,不是靠加样本量能解决的。

---

## 二、工具:能立刻做的事

### 6. 截图与演示

README 里现在挂着一个截图占位。**这是最影响别人是否愿意点开仓库的一件事。** 五张图就够:书架、读书、写作台、单句练习、逐句对照。

### 7. 更好的切分规则

现在的切分对现代白话散文效果不错。以下形态还没有专门处理:

| 文体 | 问题 |
| --- | --- |
| 古文 / 骈文 | 句读规则不同,标点体系不一样 |
| 诗歌 | 行长 vs 句长的单位问题;分行是内容还是格式 |
| 译本 | 长定语从句多,按标点切会切出很长的段 |
| 技术文档 | 代码块、列表、表格会被当正文处理 |

**这一类贡献很实际:** 导入一段切坏的文本,把切分规则改对,加上测试。

### 8. 手感与排版

写作工具的手感就是产品本身。目前已知可以改进的:

- 字号、行距、页宽的默认值,以及是否该允许用户调
- 长文导入后的进度与状态呈现
- 键盘流:不用鼠标能不能走完一轮练习
- 弱网/无网时的状态提示

### 9. 打包

现在需要装了 Python 才能跑。目标:**双击一个文件就能用,不需要装任何东西。**

| 平台 | 做法 |
| --- | --- |
| Windows | PyInstaller 单文件 exe |
| macOS | 打包成 .app,或 Homebrew formula |
| Linux | AppImage,或 Docker 镜像 |

**注意这条与"零依赖"约束的关系:** 打包工具只在构建期使用,**不得引入运行期依赖**。

### 10. 数据可携带性

- 导入已有书籍的**批**操作(现在一次一本)
- 全库导出/导入(现在可以手拷 `app/data`,但值得做成界面操作)
- 按书、按时间、按段检索历史稿子

### 11. 多人练习同一本书?

需要先想清楚:**笔记是私人的,稿子也是。** 如果要支持,得先决定共享的边界。**这个功能在讨论清楚之前不会做。**

---

## 三、明确不做

写在这里,免得有人花时间做:

| 不做 | 原因 |
| --- | --- |
| 评分 / 相似度 / "像不像作者" | 实测结论:它只会把用户推离目标作者。见 [`docs/研究/`](docs/研究/)。 |
| 自动改稿、自动补写句子 | 这个工具生成的全部内容就是"每句话一条提示"。 |
| 云端账号、同步、社交 | 本地优先是这个产品的身份,不是暂时的实现方式。 |
| 手机 App | 长文写作练习在手机上的体验不成立;网页版已经可以在手机浏览器打开。 |
| 加 pip / npm 依赖 | 十年后要还能双击打开。 |
| 订阅制、按次收费 | 许可证已经禁止商业使用,这一条是它的延伸。 |

---

## 四、已经做完的

- 切分(节/段两层,可逆性有硬断言)
- 逐句提示(一句一条,条数由句子数决定,单条字数封顶,照抄检查)
- 提示缓存(内容哈希 + 模型名 + 提示词版本)、批量备好、token 估算
- 强制延迟(读完到能写之间,默认 3 分钟)
- 写作阶段防原文泄露(服务层反查自检)
- 逐句对照(LCS 对齐、命中高亮、缺失片段清单)
- 单句练习
- 笔记(一个入口、跟着书走、可导出)
- 输入动效(canvas 叠加,四档,只对落定的字)
- 账目只记真实 `usage`
- 7 组测试、370 多项检查
- 中英双语研究文档与实测数据公开

---

## 怎么参与

1. 挑一条,在对应的 Issue 里说一声(没有就开一个)
2. 读 [CONTRIBUTING.md](CONTRIBUTING.md),特别是里面的**硬约束**
3. 动手前如果拿不准,先去 [Discussions](https://github.com/luluxiuc/Franklin-Writing/discussions) 聊

**研究类的工作(第 1–5 条)优先级最高。** 如果你只做一件事,做人类标注。
