# 更新日志

本文件记录本项目值得注意的变更。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

**关于版本号的一句实话:** 这个项目从研究方法走到可用工具只用了三天,期间重构过一次方向,没有走过正式的版本发布流程。所以第一条正式记录是 **Unreleased(准备首次公开发布)**,它把此前全部工作一次性讲清楚,而不是事后编出一串版本号。

---

## [Unreleased] — 准备首次公开发布

首次公开发布的内容包含下面全部工作。项目时间线:2026-09-23 完成研究并定方向,2026-09-23 至 09-25 建成工具。

### 研究

- **实测了 LLM 能否判断"像不像作者"。** 5 位独立裁判 / 18 个匿名文本块(8 段真实已发表散文 + 10 段模仿稿,其中 5 段植入具名缺陷) / 预注册判定标准。
  - 真实文本被误判为模仿:**0%**(5 位裁判全部 8/8)
  - 植入明显缺陷的模仿被识破:**100%**(5 位裁判全部 5/5)
  - **无缺陷的模仿被识破:0%**(多数裁决;5 位中 3 位为 0/5)
  - 裁判间全同一致率 13/18 = 72%,**分歧 100% 落在"无缺陷的模仿"上**
  - 结论:**判别力 100% 来自"有没有明显缺陷",0% 来自"像不像作者"。**
- **记录了一个更严重的发现:误判方向是系统性的。** 裁判拒绝的恰恰是作者本人的标志性手法(把作者大量使用的"记忆的元陈述"判为破绽;把作者本人的留白收束判为"感伤来得过于准时";有一次**误读原文并编造语言证据**来支撑风格判断)。
- **排除了一条替代解释。** 归一化特征对比显示模仿稿的名物密度比原文还高(9.2 vs 4.7),所以"裁判只是嫌稿子短"不成立;真正可机械区分的只有连接词与情绪词。
- **测出判错不是犹豫出来的。** 3 位有留档的裁判共判错 10 次,**全部是自报置信度 ≥ 4**,其中 1 次满分 5。
- **纯计算部分的实测(零模型调用)** 推翻了两个通用印象:"这位作者善用短句"(实测句长均值 17.8 字、最长连续短句串仅 2 句;真正的特征在小句一级,均长 7.5 字)、"他不写情绪词"(仅有 2 次且都不是陈述情绪;正确表述是"不把情绪词用作主句谓语")。
- **如实记录了研究自身的缺陷**:人类一致率未测、有 3 位裁判违规检索外部来源、同长度配对检验失效、样本量小、执行者既造材料又设计实验、**预注册规则漏掉了真正的失效模式**。见 [`docs/研究/`](docs/研究/)。
- 预注册文件、材料参数、全部分析脚本与已留档的裁判判定随仓库公开。

### 决定

- **删除了设计中的全部 AI 判断模块**:对比引擎(哪里不像/为什么/怎么改)、作者模型判断层、差距排序、收敛趋势、能力结论。
- **项目方向定为不做判断的纯工具**:系统负责切分、提示、隐藏、延迟、记录;理解与评判完全由用户完成。决策过程见 [`docs/研究/why-no-llm-judgment.md`](docs/研究/为什么不做LLM判断评分.md)。
### 新增

- **本地网页工具**(Python 标准库 + 原生前端,零依赖,可完全离线):
  - 书架 → 读书 → 写作台 → 逐句对照 的完整流程
  - 切分:节(阅读单位)+ 段(练习单位)两层,**可逆**(把每一段拼回去精确等于原文)
  - 段落默认约 300 字,可在设置里调
  - 逐句提示:一句话一条,条数由原文句子数决定;单条字数封顶;缓存键为 `原文内容哈希 + 模型名 + 提示词版本`
  - 后台批量备好整本书的提示,并可先给出 token 估算(明确标注为估算)
  - 读完到能写之间的强制延迟(默认 3 分钟,可调 0–240),等待期间可以练别的段
  - 逐句对照:用最长公共子序列对齐,多写/漏写单独标出;命中词高亮;"原文里有、你没写到的片段"原样摆出
  - 单句练习:看一条提示只写这一句,写完立刻与原文那一句并排
  - 笔记:一个入口、跟着书走,标「第 n 句」或「整段」,可导出 markdown
  - 输入动效:canvas 叠加,四档(淡入 / 升起 / 书写 / 关),选择被记住
  - 账目只记服务商回传的 `usage`,拿不到就记 0,不估算
  - 数据全在 `app/data/`,纯文本,删掉即无痕

### 修复

开发期真实抓到并修掉的问题,一并记录:

- **切分兜底**:粘贴 2000 字没有句读的内容会被当成"一段",练一次要背 2000 字。现在句内按标点切,再不行按字数硬切。
- **空白规整**:从网页复制来的文章带行首行尾空格和整行空白,会让切分产生无内容碎片、句子编号飘移。现在导入时只规整空白,**一个实字都不动**。
- **防泄露自检抓到标题泄露**:某一节的标题是从原文首句截出来的,却被挂在练习页标题栏上,等于把这一段的开头送给了用户。现在练习页不显示节标题,并且服务层在返回前会拿原文反查自己要发出去的数据。
- **提示照抄原文**:模型偶尔会把原文一小句搬进提示。现在逐条做机械检查,出现连续 ≥ 8 字与原文相同即摘掉那一条;两次都在抄就一条都不给。
- **API Key 含中文或空格**会报一个看不懂的编码错误。现在当场拦住并说明原因。
- **非 ASCII 文件名**在 `Content-Disposition` 里出错。改用 RFC 5987 的 `filename*=UTF-8''` 并附 ASCII 兜底。
- **自动保存被当成已交稿**:草稿自动保存后,用户就再也改不了、也交不了。现在状态机以"是否真的交过稿"为准。
- **动效按钮点了没反应**:`FONTS_ANIM` 声明在使用它的函数**之后**,踩了暂时性死区,函数第一行就抛异常,而界面上不显示任何提示。
- **动效关不掉**:判断"开没开"用的是 `!!S.anim`,而"关"这一档编号是 4,`!!4` 为真——选"关"反而把动效打开了。现在只走 `animOn()` 一个判断。
- **三档动效看起来一模一样**:绘制函数根本没读当前档位。现在每档有各自的画法参数,并有测试断言它们确实不同。
- 若干接口路由长度判断写错导致 404、`POST` 路由被上层吞掉、字段改位置后取值失败。

### 测试

- 7 组测试、**370 多项检查**,`python tests/run_all.py` 一次跑完。runner 自己起带假模型的服务,跑完关掉,**不联网、不花钱**。
- 其中的关键几组:
  - **切分可逆性**(原文一个字不丢)是硬断言
  - **写稿阶段任何接口都不返回原文**
  - **前端契约**:JS 引用的元素与接口是否都存在
  - **按钮点得动**:把模板真渲染出来、找出每个按钮、点了看状态有没有变
  - **输入动效**:只对落定的字做动画、各档画法确实不同、动画量不随文章长度增长、能挂能拆
  - **前端渲染**:在 Node 里真跑一遍每个页面的渲染

### 文档

- 中英双语详情页:两种语言的 README,以及研究报告、实测数据、实验数据说明的双语版本。
- [`docs/设计/`](docs/设计/):方法论、无判断版设计、工具使用说明、跑测报告。

---

## 更早(记录用)

这些工作发生在正式发布之前,不单独编号,列在这里以免历史断掉:

- **方法论文档 v0.1 → v0.2 → v0.3**:从"AI 富兰克林写作训练系统"的完整设计,逐步收敛到"代理漂移"这一核心风险的识别。v0.3 首次把"代理漂移"写成项目最危险的失效模式。
- **无判断版设计 v1.0**:删除全部 AI 判断后的产品设计,由实验结论触发。
- **命令行原型** `fk_tool/fk.py`:单文件、零模型调用、可离线,含 11 项冒烟断言与 7 项约束自检。它验证了"删除判断之后方法仍然成立",随后被网页版取代。**保留在仓库里作为记录,不再维护。**
- **指纹与反馈引擎跑测报告**:记录了手写阈值规则产生的四个结构性假阳性(把作者原文判为"偏离作者"、在错误的层级上取样、把低频特征当逐段必备项、写得更接近作者的稿子反而拿到相当的严重问题数)。这些失效证明了"用阈值冒充判断会跑偏",也是后来删除判断的早期证据。
