# 贡献指南

先说最重要的一条,它比其它所有条目都重要:

> **这个工具永远不评判写作。**
>
> 不接受任何形式的新增:评分、相似度、像不像作者、等级、排名、"你这段写得怎么样"、自动给出修改建议、自动生成用户该写的句子。
>
> 这不是偏好问题,是实测结论。依据见 [`docs/研究/`](docs/研究/)。

想改这一条?**可以,但要用数据推翻实验**,而不是用意见说服我们。请先开一个 Issue 把复现方案贴出来。

---

## 欢迎的贡献

| 方向 | 具体是什么 | 难度 |
| --- | --- | --- |
| **补实验** | 真人标注(3 位以上合格读者)、无记忆语料复测、保留原始日志重跑实验一。**这是最有价值的一类。** | 中 |
| **报告问题** | 尤其是"点了没反应""切分不对""字丢了"这三类。请附复现步骤与系统信息。 | 低 |
| **界面与排版** | 字体、行距、留白、沉浸感。写作工具的手感就是产品本身。 | 低–中 |
| **切分规则** | 古文、诗歌、译本、技术文档的适配。 | 中 |
| **打包** | Windows / macOS 单文件、Docker、Homebrew。 | 中 |
| **翻译** | 界面文案与文档的多语言版本。 | 低 |
| **文档** | 修正错字、补截图、把设计取舍讲得更清楚。 | 低 |

---

## 环境与跑测试

需要 **Python 3.10+**。只有跑前端检查时才需要 **Node 18+**。

```bash
git clone https://github.com/luluxiuc/Franklin-Writing.git
cd Franklin-Writing
python tests/run_all.py
```

`run_all.py` 会自己起一个**带假模型的服务**来跑渲染检查,跑完关掉。**不需要 API Key,不会联网,不会花钱。**

单跑某一组:

```bash
python tests/test_store.py       # 数据层
python tests/test_summary.py     # 逐句提示与缓存
python tests/test_server.py      # HTTP 端到端、防泄露
python tests/test_frontend.py    # 前端契约
node   tests/ui_check.mjs        # 按钮点了真的有反应(需先起服务)
node   tests/anim_check.mjs      # 输入动效
node   tests/render_check.mjs    # 前端渲染(需先起服务)
```

**提交 PR 前必须全绿。** 只改了文档可以例外。

---

## 硬约束(改了就等于是另一个项目)

### 一、不评判

见开头。这条没有例外,包括"只给个参考分数""只是个进度条""用户可选开启"。

### 二、提示必须一句一条

原文有几句,就有几条提示。这个不变量在提示卡测试里是硬断言:

- **条数由原文的句子数决定,不是模型说了算的。** 模型少给会如实报不对齐,多给会截断。
- **单条字数有上限**(≤4 句时 22 字,≤8 句时 18 字,更多时 15 字)。
- **不能把整段概括成几条。** 那会把原文重新组织一遍,而那个结构是模型的、不是用户的;而且极易把原文措辞带进来。

### 三、提示里出现原文的连续片段必须摘掉

任何一条提示里只要出现 **连续 ≥ 8 个字与原文逐字相同**,这条就丢掉。

**这条不只是省钱**:提示一旦给了原文的句子,用户就不是在凭记忆写,练习直接失效。阈值 `min_run=8` 可以在讨论中调整,但"必须有这道检查"不能动。

### 四、写作阶段一个字的原文都不能出现

练习页和对照页之外的任何接口,在用户交稿之前都不能返回原文。服务层有一条**反查自检**:发出去的数据会拿原文再扫一遍,发现疑似泄露就拒绝服务。

> 它真的抓到过:某一节的标题是从原文首句截出来的,而它被挂在了练习页的标题栏上,等于把这一段的开头送给了用户。**现在练习页不显示节标题,这是有意的,别加回去。**

### 五、切分必须可逆

原文只存一份,段的位置是原文里的坐标。所以**把每一段拼回去必须精确等于原文**。这是测试里的硬断言,是所有对照功能可信的前提。

### 六、零依赖

后端只用 Python 标准库。**不引入任何 pip 依赖**,包括看起来很无辜的。前端不用框架、不用构建步骤、不用 CDN。

理由:这个工具必须能在十年后、一台没有网络的旧笔记本上双击打开。它的价值有一半在"数据一直在,随时能打开"。

### 七、界面不放 emoji

用字体和排版表达层级,不用图标式 emoji。这一条是用户明确要求的。

---

## 代码风格

- **注释解释"为什么",不解释"是什么"。** 尤其要写清楚某个阈值、某个顺序是**踩过什么坑**才定下来的。这个仓库里最有价值的注释都是这类。
- 中文注释、中文文案,和现有代码保持一致。
- 不引入格式化工具和 lint 规则集。跟周围代码保持一致就行。
- **新增行为必须带测试。** 尤其是前端:光断言"数据不同"是不够的,要断言"点击真的通到了代码里"。

### 前端的三条经验

都是真出过事故才总结出来的:

**一、动画绝不碰输入框里的文字。**
canvas 盖在上面画,字体字号从输入框上量。给每个字包一个 DOM 元素会毁掉中文输入法、选区和撤销,而且写到一千字就卡。

**二、只对"落定的字"做动画。**
中文打字时输入框里会先出现拼音和候选词。跟着每次输入事件做动画,动效就散在一堆中间状态上,看着乱也不明显。要等输入法定字那一刻。

**三、`const` 声明要放在使用它的函数之前。**
听起来很蠢,但确实发生过:`FONTS_ANIM` 声明在使用它的函数后面,踩了暂时性死区,函数第一行就抛异常,界面上表现为"**点了按钮没反应**",而且不弹任何提示。现在有一组测试专门渲染模板、找出按钮、点它、再看状态有没有变。

---

## 提交流程

1. **先开 Issue 再动手**,除非是明显的笔误或小修复。这样能避免你写完一个我们不接受的东西。
2. Fork,从 `main` 切分支。分支名随便,能看懂就行。
3. 改完跑 `python tests/run_all.py`,全绿。
4. 提 PR,写清楚三件事:
   - **改了什么**(具体到行为)
   - **为什么**(如果是修 bug,写清复现步骤)
   - **怎么验的**(哪个测试、或者你手动怎么试的)
5. 新增或修改行为时,**同一个 PR 里带上测试**。

**关于提交信息:** 用中文或英文都行,能看懂就行。不强制任何格式。

---

## 不会被接受的贡献

| 提议 | 为什么不行 |
| --- | --- |
| 加评分 / 相似度 / "像不像作者" | 实测结论:它只会把用户推离目标作者。见 [`docs/研究/`](docs/研究/)。 |
| 自动给用户改稿、补写句子 | 这个工具生成的全部内容就是"每句话一条提示"。 |
| 加登录、云同步、账号体系 | 本地优先是这个产品的身份,不是暂时的实现方式。 |
| 引入 web 框架 / 打包器 / 构建步骤 | 违背"十年后还能双击打开"。 |
| 加 pip 依赖 | 同上。 |
| 大量 emoji / 图标式界面 | 用户明确否掉了。 |
| 把提示改成整段概括 | 与富兰克林本人的做法和这个练习的目的都相反。 |
| 让用户"可选开启"以上任意一项 | "可选"不改变它会把一部分用户带偏这件事。 |

---

## 提 Issue

用 [模板](https://github.com/luluxiuc/Franklin-Writing/issues/new/choose)。**有想法但还没想清楚,去 [Discussions](https://github.com/luluxiuc/Franklin-Writing/discussions)**,不要急着开 Issue。

报告 bug 时这些信息很有用:

- 系统与 Python 版本
- 复现步骤(能稳定复现最好)
- 你导入的是什么类型的文本(纯文本?网页粘的?古文?)
- 浏览器控制台的红色报错(如果是前端问题)
- **不要贴 API Key。**

---

## 发布

维护者负责。流程:

1. `python tests/run_all.py` 全绿
2. 更新 [`CHANGELOG.md`](CHANGELOG.md)
3. 打 tag 并写 Release 说明

---

## 行为准则

参与本项目即表示你同意遵守 [行为准则](CODE_OF_CONDUCT.md)。

## 许可证

本项目采用 [PolyForm Noncommercial License 1.0.0](LICENSE)。**提交贡献即表示你同意你的贡献以同一许可证发布。**
