# 安全政策

## 这个工具的安全模型

先讲清楚它是什么,这样你能自己判断哪些是风险、哪些不是。

**这是一个本地工具。** 服务只监听 `127.0.0.1`,同一个局域网里的其他机器也连不上。它没有账号体系、没有云端、没有遥测、没有自动更新。

**你的数据全在本地:**

```
app/data/index.json        书目、稿子、笔记、设置(含 API Key)
app/data/index.json.bak    上一版
app/data/texts/<id>.txt    每本书的原文
app/data/summaries.json    逐句提示的缓存
```

**唯一的对外网络请求,是你在设置里填了 API Key 之后,由提示功能发出的:**

- 请求发往你自己选的服务商地址(`base_url`),内容只有**被要求做提示的那一段原文**;
- 你的稿子、笔记、书目**不会被发送**;
- 不填 Key = 一次外发请求都没有。

---

## 报告漏洞

**请不要用公开 Issue 报告安全漏洞。**

用 GitHub 的私密漏洞报告:在仓库的 **Security** 标签页点 **Report a vulnerability**。若无法使用,请私下联系 [@luluxiuc](https://github.com/luluxiuc)。

请包含:

- 受影响的版本或提交
- 复现步骤
- 影响范围(能读到什么、能改到什么)
- 如果有,附上建议的修复方向

**我们会尽力及时回应。** 这是一个个人维护的项目,不承诺 SLA,但我们承诺:收到后会认真看、处理完之前不公开、修好之后在 [`CHANGELOG.md`](CHANGELOG.md) 里写清楚。

---

## 我们特别关心的几类问题

按严重程度排序:

### 1. 写作阶段泄露原文

**这是本项目最严重的一类漏洞。** 用户在凭记忆写作时,任何接口、任何页面、任何报错信息里出现原文,都直接使练习失效。

服务层已经有一条反查自检(发出去的数据会拿原文再扫一遍),但它不可能覆盖所有路径。**如果你找到绕过它的方式,请报告。**

### 2. API Key 泄露

- Key 进日志、进报错信息、进导出文件
- Key 被发送到 `base_url` 之外的地址
- 网页端任何地方回显了 Key

> 顺带说明:`app/data/index.json` 以**明文**保存 API Key。这是有意的——本工具不做加密,因为在一台只有你能登录的机器上,加密只会制造"它被保护了"的错觉。**请按保护任何明文凭据的方式保护这个文件。** 如果你认为这个决定应该改,欢迎开 Discussion 讨论。

### 3. 本地服务被非本机访问

服务绑定 `127.0.0.1`。如果你找到让它监听 `0.0.0.0`、或被 DNS 重绑定攻击绕过的路径,请报告。

### 4. 恶意导入文本导致的问题

路径穿越、超大文件导致内存耗尽、编码处理不当导致崩溃。**注意:文本内容本身不会被当作代码执行**——没有 `eval`、没有模板注入、没有把导入内容拼进任何命令。若你发现相反的情况,请报告。

### 5. 依赖

本项目**没有第三方依赖**(仅 Python 标准库 + 原生前端)。所以不存在供应链依赖风险——**但也请帮我们盯住这条线**:任何"就加一个小依赖"的 PR 都会削弱这个性质,值得在评审时质疑。

---

## 不在范围内

- **你导入的文本本身的版权问题。** 工具只是个文本处理器。
- **你选的那家模型服务商如何处理你发过去的那一段原文。** 那是你和它之间的事;请读它的隐私政策。**如果你在意,就不要填 Key** —— 不填也能完整使用这个工具的所有训练功能。
- **你机器上的其他程序读到了 `app/data/index.json`。** 本机文件权限由你的操作系统负责。
- **社交工程。** 比如有人冒充维护者向你要 Key。**我们永远不会向你索要 API Key。**

---

## 支持范围

只有仓库最新 `main` 分支的代码在支持范围内。请在报告前先确认问题在最新代码上仍然存在。
